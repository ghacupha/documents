version: '2'
services:
  documents-mongodb:
    image: mongo:4.0.16
    ports:
      - '27017:27017'
    command: mongos --configdb documents-mongodb-config
  documents-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  documents-mongodb-config:
    image: mongo:4.0.16
    container_name: documents-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
